# No environment variables required for the Python YOLO detection server.
# The server runs locally on http://127.0.0.1:5001 with no external dependencies.
#
# For the Convex auth backend environment variables, see ui/server/.env.example
